# 第二轮原型验证记录

- 日期：2026-09-05。
- 范围：完整合成集合上的聚合概览、全范围搜索、共享成员与分批一跳探索。
- 状态：构建、数据投影与浏览器任务走查通过。完整仓库 Gate 在 catalog audit 阶段停止；正式插件及真机未验证。

## 数据与投影

普通、密集、大图、空图和读取失败分别检查 V12、V13，共 10 组完整 fixture。
使用冻结对象执行查询，前后完整集合的 SHA-256 相同。
8,448 份概览/局部图投影均通过唯一身份、边端点、分页并集完整性和预算检查。
最大投影为 16 个节点、18 条边、13,266 字节 JSON；完整目录不作为图响应。

密集图有 160 段记忆、4 个组、404 条边；其中 81 段属于多个组，最大组有 114 个成员。
大图有 1,000 段记忆、8 个组、2,608 条边；其中 568 段属于多个组，最大组有 654 个成员。
每个组的计数与唯一成员集合匹配，分页访问的并集与完整邻居集合一致。

全范围搜索找到初始未展开的 `dense-143`（北极星计划）与 `large-913`（纸飞机项目）。
搜索分页的结果并集完整；`dense-12` 在两个关联组中使用同一个节点身份。

证据：本机隔离目录 `artifacts/prototype-v2-qa/projections.json`。

## 浏览器任务走查

独立浏览器复核检查三个任务：查找未展开记忆、识别共享成员、高连接组翻页后进入详情并返回。
在桌面 1440×1000、1440×900 与启用触摸的 390×844 视口中实际检查截图。
14 / 14 项界面场景与 4 / 4 项触控检查通过，覆盖三任务、折叠/分页、共享来源、聚焦、搜索、详情与重载恢复。

| 检查 | 结果 |
|---|---|
| 完整目录中的未展开记忆 | `dense-143` 与 `large-913` 均可搜索并查看来源 |
| 共享成员 | 经 `dh0`、`dh3` 都能定位同一个 `dense-12`，详情显示两个所属组 |
| 高连接组 | 114 邻居按批访问；第二页进入详情再返回，页码和选择保留 |
| 局部与搜索状态 | 筛选、聚焦、重定中心、搜索页码、概览页码和刷新页面续看通过 |
| 触摸主链路 | 12 次概览成员点击/返回、6 次局部→详情→返回无穿透 |
| 缩放和手势 | 65% 下附近命中与最近中心选择有效；真实触摸拖动、双指缩放到 193% 与重置通过 |
| 长原文与剪贴板 | 正文约 70 px 展开至 257 px，复制包含全部合成原文 |
| 失败与新发布提示 | 空图、失败重试、刷新保留身份通过；新展开、翻页、重心和详情在刷新前阻断 |
| 新发布时的关键词输入 | 允许在已加载的旧版本示例目录内筛选；打开结果仍要求刷新，未混入新版本数据 |
| 浏览器错误 | 最终检查未发现 console error、pageerror 或 HTTP 错误 |

本轮复核发现并修复两处界面问题：探索路径的短标签补足 44×44 px 触控尺寸；
千条样例展开时，未选中的关联组节点缩小以避免两排组名与菱形重叠。
定向复验显示包围盒零相交，实际截图可读；9 次组切换与 2 次成员点击/返回通过。

本机完整脚本、截图与报告位于
`/private/tmp/roxy-akasha-memory-graph-d2guqc4c/artifacts/prototype-v2-qa/`。
临时目录可过期，本页保留交付结论与范围。

这些是自动化和独立界面检查，不等于真实用户可用性研究。
尚未测量人的查找耗时、误解概率或大规模真实图的认知负担。

## 集成与 Gate

当前只是独立原型，未接入真实插件接口、来源库或 Android 设备。
使用 Python 3.12 执行 `docker/debug/gate.py run --base origin/main`，退出码 2；
随后 `gate.py audit` 退出码 1，报告为
`docker/debug/reports/change-gate/20260905-165914-41249f19/inventory.json`。
独立设计目录仍有 7 个文件未被 capability catalog 映射：manifest、app bundle、build-info、
构建脚本、第一轮对照 bundle、data.js 和 projection.js。
本轮保留现有 Gate 合同，公开场景未开始执行，不能声明完整 Gate 通过。
真实集成前置问题见[准备记录](../../../docs/design/akasha-mobile-memory-graph-readiness.md)。
第一轮结果见[归档记录](QA-round-one.md)，不作为第二轮通过的证据。

## 最终资源身份

```text
index.html  7c65d3ade67b19417a36a5664fc3d40dd87ddcaed7476878c6bc29415bfcd547
styles.css  11b5faa248c2ab56b4b1a694ec162f68de172493a54bdbd0a0716ab48b8cb370
app.js  a2b7be4ed81b6fa9bb8a3e9e314fc054f76996c89a8f816311a8cae70b9a567b
src/data.js  c334daf4639c24ba4be96f5d5c5b20598e5dd8082a81b97cbbcd5f1a4e14a6cd
src/projection.js  9edafa724638d658f232a6a4eb7e4f39f6119b1e80a7982beb7e46e41dbdcf10
```
