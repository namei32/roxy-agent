min_version = "2026.8.3"

[tools]
node = "22.23.1"
npm = "10.9.8"
python = "3.14.6"
uv = "0.12.3"
"npm:@jackwener/opencli" = "1.8.6"
opencode = "1.18.15"

[tasks.doctor]
description = "Verify the shared development and Host Bridge toolchain"
run = [
  "python --version",
  "node --version",
  "npm --version",
  "uv --version",
  "opencli --version",
  "opencode --version",
]
