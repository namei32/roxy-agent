---
name: akashic-call
description: 兼容旧的 Akashic 调用 Skill 名称；请按当前 Roxy 调用合同执行。
metadata: {"akashic": {"always": false}}
---

# Akashic 调用兼容入口

这是保留给既有自动化与历史提示词的兼容名称。请读取并遵循
[`../roxy-call/SKILL.md`](../roxy-call/SKILL.md)：新的 SDK、环境变量、默认 Socket 和文档均使用
Roxy 名称；旧 `akashic_sdk`、`AKASHIC_*` 与 `akashic.sock` 只在运行时兼容层中继续可用。
